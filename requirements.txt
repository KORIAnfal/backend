
# Backend Libraries
Flask==2.3.0
werkzeug==2.1.0
flask-cors==5.0.1
Flask-SQLAlchemy==2.5.1
flask-migrate==3.1.0
Flask-RESTful==0.3.9
requests==2.28.1
python-dotenv==0.21.0
debugpy==1.6.3

# Utilities
attrs==25.3.0
bcrypt==4.0.1             # If you're using bcrypt for password hashing
beautifulsoup4==4.11.1    # For parsing HTML (if needed)
cloudpickle==2.2.1        # For serializing Python objects (useful for caching)
cffi==1.15.1             # For handling C bindings
click==8.1.3             # For command-line interfaces
colorama==0.4.6          # For colored terminal text
cryptography==3.4.8      # For encryption (if needed)
flask-login==0.5.0       # If you're using Flask for user authentication
fsspec==2023.1.0         # For filesystem specification
greenlet==2.0.1          # For asynchronous programming (if needed)
idna==3.4                # For handling international domain names
importlib-metadata==4.11.4 # For backward compatibility with Python versions < 3.8
pyyaml==6.0              # For working with YAML files (if needed)
